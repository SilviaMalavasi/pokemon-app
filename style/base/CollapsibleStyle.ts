import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  heading: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
    paddingVertical: 4,
  },
  content: {
    marginTop: 6,
    marginLeft: 24,
    marginBottom: 12,
  },
});

export default styles;
