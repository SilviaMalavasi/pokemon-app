# Maestro test for ThemedText component
appId: com.silviamalavasi.pokedeckbuilder
---
# Launch the app (assumes a screen with ThemedText is accessible)
- launchApp

# Navigate to the screen containing ThemedText (update as needed)
# - tapOn: "YourScreenWithThemedText"

# Assert the text is visible (replace 'Sample Text' with the actual text rendered by ThemedText)
- assertVisible: "Sample Text"
# (Optional) Assert different text types/styles if rendered
# - assertVisible: "Another Text Variant"
