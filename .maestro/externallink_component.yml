# Maestro test for ExternalLink component
appId: com.silviamalavasi.pokedeckbuilder
---
# Launch the app (assumes a screen with ExternalLink is accessible)
- launchApp

# Navigate to the screen containing ExternalLink (update as needed)
# - tapOn: "YourScreenWithExternalLink"

# Assert the link is visible (update the link text as needed)
- assertVisible: "Link Text Here"

# Tap the link
- tapOn: "Link Text Here"
# (Optional) Assert that the browser is opened or a confirmation appears
# - assertVisible: "External Browser Opened" # Only if your app shows a confirmation
