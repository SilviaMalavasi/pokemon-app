# Maestro test for ThemedModal component
appId: com.silviamalavasi.pokedeckbuilder
---
# Launch the app (assumes a screen with ThemedModal is accessible)
- launchApp

# Navigate to the screen containing ThemedModal (update as needed)
# - tapOn: "YourScreenWithThemedModal"

# Assert the modal is visible (replace 'Modal Title' or a unique text in your modal)
- assertVisible: "Modal Title"

# Tap the close button (replace 'Close' with the actual button text if different)
- tapOn: "Close"
# (Optional) Assert the modal is no longer visible
# - assertNotVisible: "Modal Title"
