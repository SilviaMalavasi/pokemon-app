# Maestro test for ThemedChip component
appId: com.silviamalavasi.pokedeckbuilder
---
# Launch the app (assumes a screen with ThemedChip is accessible)
- launchApp

# Navigate to the screen containing ThemedChip (update as needed)
# - tapOn: "YourScreenWithThemedChip"

# Assert the chip label is visible (replace 'Chip Label' with the actual label)
- assertVisible: "Chip Label"

# Tap the chip to select or activate it
- tapOn: "Chip Label"
# (Optional) Assert the chip is now selected or changed if the UI provides feedback
# - assertVisible: "Selected State Indicator" # e.g., style or icon change
